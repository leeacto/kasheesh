# Getting Started
1. Requirements:
* Python v3.8
2. Run `python3 -m venv .venv` to create virtual environment
3. Run `. .venv/bin/activate` to activate environment
4. Run `flask --app api run` to run local server at http://127.0.0.1:5000
